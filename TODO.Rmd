---
title: "TODO"
output: html_document
editor_options: 
  markdown: 
    wrap: 72
---

## functions to add

**v0.1.0-beta** **Public release**

1.  Alex to dos

    1.  fix trait-reading code in `runOccJSDM()`: it checks
        `data$traits`, not `data$traitsMatrix` or the unused
        `traitsMatrix` function argument. This currently works only
        because R's `$` partially matches list names (`traitsMatrix` is
        the only element of `data` starting with `"traits"`), which is
        fragile. Fix the source to reference `data$traitsMatrix`
        explicitly, and either wire up the `traitsMatrix` argument or
        remove it from the signature.

    2.  ~~fix `model`-used-before-assignment bug in `runOccJSDM()`
        (`R/runOccJSDM.R`): the "read OTU data" block (around line 459)
        references `model` to decide whether to truncate reads at
        `threshold`, but `model <- inferDataModel(data)` isn't assigned
        until later, in the "data structure infer" block (around line
        522). This throws `object 'model' not found` on every call.
        Confirmed still present after the `a9700ab` refactor. Fix by
        moving the `inferDataModel(data)` call earlier, before its first
        use.~~ **fixed**: `model <- inferDataModel(data)` moved up to
        line 451

    3.  ~~gitignore and stop tracking `src/occPlus.dll` (also filename
        should be changed on Alex's end)~~

    4.  `predictNewSites(fitmodel)` for non-surveyed sites

    5.  fix `Xs` recycling bug in `simulateOccJSDMData()`
        (`R/simulateData.R`): for the occupancy/two-stage models,
        `data_info` binds `Xs = Xs` (the raw `n x 2` site-coordinate
        matrix) directly, without indexing it by `idx_z_k` the way
        `X_psi` is (`X_psi = X_psi[idx_z_k,]`). Since `Xs` has fewer
        rows than `data_info` (`n` vs. `sum(K)`), R silently recycles
        it, so the resulting `info$Xs.1`/`Xs.2` columns do not
        correspond to the correct site per row. Confirmed live: for a
        given `Site`, the replicate rows show as many distinct
        `(Xs.1, Xs.2)` pairs as there are replicates, instead of one
        coordinate pair repeated. There is also currently no bug-free
        way to recover per-site coordinates from
        `simulateOccJSDMData()`'s output for these models (`data_list`
        only exposes `info`/`OTU`/`traits`; `true_params` doesn't carry
        `Xs` either). Fix: index `Xs` by `idx_z_k` before binding into
        `data_info`, as done for `X_psi`.

    6.  ~~fix copy-paste bug in `computeMinESS()` (`R/diagnostics.R`,
        introduced in `af5ebe3`)~~ -- **fixed**: `computeMinESS()` now
        computes `ESS_beta0psi` from `beta0_psi_output` directly and
        includes it in the final `min()`

2.  Doug to dos

    1.  purge `data/traitdata_caiwang.rdata` from git history (need to
        coordinate with Alex because he will need to re-clone)

    2.  ~~model diagnostics functions from GLGS-eDNA repo~~ -- **done**:
        `computeRhat()`, `summarisePosterior()`, `plotTraceplot()`, and
        `returnConvergenceDiagnostics()` added to `R/diagnostics.R` and
        exported (Rhat, tidy posterior summary table, trace plots).
        `compare_to_true()`/`plot_estimated_vs_true()` (simulation-truth
        comparison, requiring `true_params`) were **not** ported -- left
        as a possible future item. A "Model diagnostics" vignette
        section and a testthat test were also deferred.

**MEE paper**

1.  Doug to dos

    1.  **reproduce all Ecoletts results as a test of the package and
        decide whether to include in repo**

2.  Alex to dos

    1.  Overleaf document for the occJSDM math, which we will add as a
        markdown vignette

    2.  GAMs for JSDM

    3.  ability to analyse count data

    4.  `plotCumulativeSpeciesDetections()` for different values of M.

    5.  scenario for source-sink inference (sites where env covariate
        coeffcients are negative but spatial covariate coefficients are
        positive), using an explicit source-sink simulation

    6.  remove effect of space on environmental covariates. remove the
        effect of unobserved environmental on observed environmental
        covariates and space. thus, adding factors (unobserved env
        covariates) doesn't change the effect of observed env covariates

    7.  site variance partitioning to complement the species variation
        partitioning (see Leibold et al., Cai et al.). Consider calling
        it **variation** partitioning.

**Future versions**

1.  use spike-in to estimate abundance change (i.e. eDNAPlus)

2.  model selection of environmental, spatial covariates via
    regularisation/shrinkage, which would be useful with e.g. geospatial
    foundation model embeddings as env covariates
