# Non-spatial sampling-design investigation, 19 September 2026

User-approved question: compare 2 versus 4 field samples and 100 versus 300 sites, changing one thing at a time and keeping six PCR replicates per primer; add a control supplying otherwise-unmeasured site effects.

Use the archived, installed 80d449d package from the existing investigation. Keep default priors, 10 species, two primers, two environmental covariates, two observed and two latent traits, and two residual site factors. Run ten paired communities under each of the existing low/high contamination scenarios. Retain existing 100-site, two-sample K6 fits as the baseline. No spatial effects. No release threshold is introduced. Unchanged priors means the package defaults in each fitted design's standardized covariate coordinates; adding sites or field samples changes the corresponding empirical centres and scales, so the defaults' raw-scale implications also change. The true biological relationships on the original covariate scale are preserved.

Create 60 additional fits: 20 with M=4,n=100; 20 with M=2,n=300; 20 with true U supplied for M=2,n=100. Use the original 2-chain, 3000-burn, 5000-retained schedule initially, one sampler thread per fit. Retain warnings, rank-normalized Rhat, ESS and Monte Carlo SE; perform targeted longer checks only where diagnostics could affect conclusions. Short setup fits are excluded from final results.

Nesting: preserve original species traits, response coefficients on their original environmental scale, detection/collection parameters, occupied states, field samples and PCR outcomes. For M=4 append two field samples per existing site, retaining both original samples. For n=300 append 200 sites with environmental covariates drawn from the original Normal(0,10) distribution and non-spatial site factors from Normal(0,1). Preserve the original 100 sites exactly. Transform environmental coefficients to each fitted standardized scale for scoring; biological relationships on the original scale remain unchanged. Add field covariates from Normal(0,1), collection states using the existing theta/theta0 process, and PCR read counts using the existing event-plus-read-intensity process. Derive new stream seeds from the original qtest simulation seed using explicit offsets, share augmentation draws across low/high contamination where appropriate, and record them. The existing 20 baseline fits are never overwritten.

Control: condition on the true two-column site factor matrix U, but estimate species loadings L and every other parameter using the same original priors and conditional updates. Clone the installed runOccJSDM and update_jSDMcoef functions into a private R environment; replace only the initialization of U, its read at the beginning of each update, and its sampled update with known_U. Do not modify the installed package, namespace, or C++ functions. Standard orthogonal output reparameterization is allowed and preserves U L. Verify that every retained U draw spans the supplied U with an orthogonal transformation, and that reconstructed occupancy matches saved means. This is an idealized conditional diagnostic, not a supported public fitting option; it supplies U, not L, U L, z, p, q or occupancy probabilities.

Primary comparison: paired changes in occupancy signed errors and MAE at the same original 100 sites, separately for true probabilities below .2, .2-.8, and above .8, plus all probabilities. Secondary: all 300 sampled sites in the larger design; collection probabilities, p/q, species prevalence and error; dataset variation and Monte Carlo uncertainty. Aggregate each dataset equally, calculate paired standard errors over ten communities, and aggregate squared errors before taking an RMSE. Report costs (field samples/PCR reactions), different information supplied by known-U, and limits of the single rare case. Keep raw inputs, fits, seeds and hashes locally; put compact comparisons and scripts in the existing evidence branch after verification.

## Execution ledger

- [x] Read frozen fitting/generation/scoring code and verify clean existing evidence branch.
- [x] Implement input nesting and conditional-U control with independent assertions.
- [x] Execute six small setup fits and validate indexing, transforms, supplied-U subspaces and reconstructed probabilities. Short chains produced expected convergence warnings and are excluded.
- [x] Run all 60 fits, preserving progress and diagnostics. All 60 completed; eight targeted longer checks also completed without warnings.
- [x] Independently recompute paired summaries; assess flagged fits. Longer checks use four chains, 6,000 burn-in and 12,000 retained iterations per chain; preserve the original fits and show the effect of substituting longer fits.
- [x] Produce three figures and a clear report, update TODO, and prepare verified compact artifacts for the existing evidence PR.

## Independent review

A separate read-only reviewer verified all 60 input datasets, all 30 low/high contamination pairs, preservation of original observations and raw-scale environmental predictors, unchanged installed function bodies, and every retained U transformation in one complete known-U fit. No consequential construction or conditional-target defect was found. The summary was extended to retain collection-probability errors at the original 200 field samples and species errors at the original 100 sites. Numerical Monte Carlo standard errors refer to means and signed errors, not to MAEs. These construction checks do not establish convergence of unfinished fits.

## Longer-check selection

The completed 60-fit screen selected all eight new fits with an individual scored-parameter Rhat above 1.1 or a primary, original-100-site occupancy-group Rhat above 1.05: `qnear_K6-field4-02`, `qnear_K6-field4-07`, `qnear_K6-sites300-02`, `qfar_K6-field4-09`, and `qfar_K6-sites300-01`, `03`, `07`, `09`. These numerical screening values are not ecological release thresholds. Two secondary all-300-site groups remain above the screen: the low-probability group in `qnear_K6-sites300-10` has Rhat 1.051, and the high-probability group in `qfar_K6-sites300-08` has Rhat 1.055. Their original-100-site groups are below the screen. These are retained as unrechecked secondary diagnostics rather than expanding the run for borderline, secondary-only flags. Report both remaining diagnostics explicitly.
